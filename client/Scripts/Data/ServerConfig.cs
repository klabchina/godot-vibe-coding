namespace Game.Data;

/// <summary>
/// Server tick rate and timing constants.
/// </summary>
public static class ServerConfig
{
    /// <summary>Server runs at 20 ticks/s (50ms per tick)</summary>
    public const float ServerFrameTime = 0.05f;
}
