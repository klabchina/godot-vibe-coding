syntax = "proto3";

package summer;
option csharp_namespace = "Server.Proto";

// Client → Server — 玩家准备
message PlayerReady {
    string room_id = 1;
}

// Server → Client — 游戏开始（全员 ready 后广播）
message GameStart {
    string room_id = 1;
    int32 random_seed = 2;
}
